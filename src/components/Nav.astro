---
// No special imports needed for Tailwind CSS if you have it set up globally.
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Green Fire Effect Navigation</title>
    <link href="/styles/global.css" rel="stylesheet">
    <style>
        /* Green fire animation */
        @keyframes green-fire {
            0% {
                text-shadow: 0 0 4px #00ff00, 0 0 6px #00ff00, 0 0 8px #00ff00, 0 0 10px #00ff00;
            }
            50% {
                text-shadow: 0 0 6px #00ff00, 0 0 10px #00ff00, 0 0 14px #00ff00, 0 0 18px #00ff00;
            }
            100% {
                text-shadow: 0 0 4px #00ff00, 0 0 6px #00ff00, 0 0 8px #00ff00, 0 0 10px #00ff00;
            }
        }

        /* Apply the green fire effect on hover */
        .fire-effect {
            display: inline-block; /* Ensure the effect is applied properly */
        }

        .fire-effect:hover {
            animation: green-fire 1s infinite;
            color: #00ff00; /* Optional: Change text color on hover to green */
        }
    </style>
</head>
<body>
    <nav class="bg-transparent fixed top-0 left-0 right-0 z-50">
        <div class="container mx-auto flex items-center justify-between p-4">
            <!-- Logo -->
            <div class="text-white text-2xl font-bold">
                <a href="#"><span class="text-white">MF</span>
                <span class="text-green-800">DOOM</span></a>
            </div>

            <!-- Navigation Links -->
            <div class="hidden md:flex space-x-6">
                <a href="/" class="text-white fire-effect hover:text-green-800 transition-all duration-300">Home</a>
                <a href="/about" class="text-white fire-effect hover:text-green-800 transition-all duration-300">About</a>
                <a href="/gallery" class="text-white fire-effect hover:text-green-800 transition-all duration-300">Gallery</a>
                <a href="/albums" class="text-white fire-effect hover:text-green-800 transition-all duration-300">Albums</a>
            </div>
        </div>
    </nav>
</body>
</html>
